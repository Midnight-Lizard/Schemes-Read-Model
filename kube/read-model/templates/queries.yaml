apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "name" . }}-queries
  labels:
    app: {{ template "name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  queries: |-
{{- $idt := 4 }}
{{- if .Values.dropAll }}
{{ (.Files.Get "queries/__DROP_ALL.sql") | indent $idt }}
{{- end }}
{{- $root := . }}
{{- range $path, $bytes := .Files.Glob "queries/A*" }}
{{ ($root.Files.Get $path) | indent $idt }}
{{- end }}